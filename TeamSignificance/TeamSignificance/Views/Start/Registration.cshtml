@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="centered flex">
        <div>
            <div>
                <h3>Nickname:</h3>
                <div>
                    @Html.TextBox("Nickname",null, new { @class = "field" })
                </div>
            </div>

            <div>
                <h3>Password:</h3>
                <div>
                    @Html.Password("Password",null, new { @class = "field" })
                </div>
            </div>

            <div>
                <h3>Confirm password:</h3>
                <div>
                    @Html.Password("ConfirmPassword",null, new { @class = "field" })
                </div>
            </div>

            <div class="buttons centered">
                    <input type="submit" value="Registrate" />
            </div>
        </div>
        <div class="validation-errors">
            @Html.ValidationSummary()
        </div>
    </div>
}